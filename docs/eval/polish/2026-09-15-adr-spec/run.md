# 运行记录：12 份 ADR 加 `spec/README.md`、`.agents/skills/README.md`，2026-09-15

| 项 | 值 |
| --- | --- |
| 基线 commit (`before/` 出处) | `787d591` |
| 命令 | 逐个 `limae polish --share-repo-with-engine <file>`，全部跑完再一次 `limae --fix <全部>` |
| 引擎 | `auto` (未指定 `--engine`) |
| prompt 最后一次改动 | `08d208a` (`spec/polish/general.md`、`spec/polish/zh.md`) |
| 词典最后一次改动 | `3bb36db` (`spec/lexicon/zh.toml`) |
| 改动量 | 14 个文件，265 行 |
| `--fix` 是否动过 | **是** —— 修了 `docs/adr/0003-rule-flags-and-config.md` 与 `docs/adr/0006-rule-grading-fixability-severity-experimental.md` 两份 |

## 这一批为什么是这 14 份

排除的三类，理由各不相同：

- **`spec/polish/**` 与 `spec/skill/**`** —— prompt 与 skill 的源文件。改它们的措辞就是改行为，prompt 的措辞就是它的语义。
- **`spec/rules.md`** —— 正文里有大量作为负面案例展示的坏句子，而「逐字保留」那一节护得住代码块与行内代码、护不住写在正文里的负面案例。
- **含「门」的 10 个文件** —— 「门」是词典收录的词，模型会把它换成「检查」；这是贯穿全仓的术语改动，等用户表态。

## 机械核对 (前后必须逐字相同的那些)

行内代码 span 与数字串在 14 个文件上全部一致，差异数 0。**这个判据验过对照臂**：往 `0002` 注入一处数字改动与一处行内代码改动，两类都报出差异；还原后复核通过。

## 词典收词的前后计数

| 词 | 前 | 后 |
| --- | --- | --- |
| 正本 | 15 | 2 → 手工补掉后 0 |
| 长出 | 2 | 0 |
| 兜住 | 1 | 0 |
| 探针 | 1 | 1 |

「探针」那处留着是**对的**：它说的是实验里真实存在的那个探针文件 (`docs/adr/0017-polish-file-mode.md`)，`spec/polish/zh.md` 明写「说的是字面动作就留着」。

## 合入前的手工修正

`after/` 是**模型的纯产物**。进仓的文件在它之上只多一处：

| 文件 | 模型写的 (漏改) | 改成 |
| --- | --- | --- |
| `docs/adr/0009-polish-hook-contract.md` | ……的内容由 ADR-0016 §一 接过并成为**正本**，本文不再是 hook 合同的**正本**。 | ……的内容由 ADR-0016 §一 接过，此后**以它为准**；hook 合同不再**以本文为准**。 |

这是漏改，不是改错 —— 同一行两个「正本」都没动。上一批 (README) 的毛病是**换错**方向 (两处被换成「见」「都在」、丢了断言)，这一批是**漏换**。两种都记着，将来看它是不是同一个原因。

## 裁决

留白。裁决人是用户；判据不在这里定义，用户怎么说就是什么。记裁决时写在下面，注明日期，照抄原话不要改写。
