# Tracker

backlog 的正本，由 `limae-orchestra` 在合入后记账 (全局守则「多 agent 协作」)。规则语义的正本在 `spec/rules.md`，这里只记还没做的事与一句话去向；证据链在 `docs/research/`。

## 规则与配置

- **链接空格 flag**：已建成 zh-typography-9、默认关 (调研 §3.10 自标争议)；留此备查，默认值等使用反馈再议。
- **`bracket_style = "contextual"`**：括号策略第三档「有中文全角、纯英文半角」，依据是 yikeke / FEX (调研 §3.4；国标的立场是一律全角，不是此档)。debate verdict 已裁决：默认维持 `half` (一律半角，即今天的 zh-typography-2 + zh-typography-3)；最小规范 —— 由一个策略规则处理**同一行内、非链接语法的配对括号** (`](` 照旧豁免)，括号内容剔除行内代码 span 后含 CJK 则用全角括号且外侧无空格、否则用半角括号且外侧一空格，嵌套括号从内向外独立判定，未配对或跨行的括号不报不修，zh-typography-2 / zh-typography-3 既有的 `disable` 语义保持兼容。
- **裸日文段落的语言探测 (P2)**：v0.3.1 只豁免了含假名的引用 span (`spec/rules.md`「全局豁免」第 4 条)，没有括号包裹的裸日文段落照旧按中文排版规则处理；要不要按行 / 按段探测日文并整体豁免，待有实际需求再定案。现象：不在「」『』《》内的裸日文专名 (如自造例 `サンプルIT推進部` 这种形态) 仍被 zh-typography-4 报并插空格；消费方 wealth-management 2026-08-31 反馈，非阻塞，wm 暂以给专名补「」规避。单点逃生口已有：v0.5.0 的行内指令 `<!-- limae-disable-next-line zh-typography-4 -->` (`spec/rules.md`「行内指令」，黄金 case `inline-disable-next-line` 就带这个例子)。仍未定的候选修法 (wm 建议)：探测独立连续假名子串，跳过该子串邻接边界的 zh-typography-4 (日文正字法本就不在 CJK–拉丁边界空格)；随其它规则改动一并走 spec 先行流程，不单独定案。
- **`quote_style` 实现**：检测与转换，语义已在 `spec/rules.md`「规划中的键」定案，新规则 id 届时分配。
- **`quote_style` majority 档**：仿 pyink majority-quotes，按文档内多数引号风格统一，作 corner / curly 之外的第三档。
- **仓内自用的词撞上词典自己的收词**：词典把「正本」收作要避免的 AI 中文 (`spec/lexicon/zh.toml`)，而本仓自己的 Markdown 里有 38 处在用它 (2026-09-03 数，散在 35 行上；`README.md`、`spec/README.md`、`spec/rules.md`、`docs/tracker.md`、多份 ADR 与调研)，包括 tracker 开头那句「backlog 的正本」。计数只算 Markdown —— 词典条目自身、`site/index.html` 这份生成产物、`spec/wordlists/` 的注释与 fixture 不在内。这是 dogfooding 上的自相矛盾：本仓用自己的 linter 检查自己的 Markdown，词典却没有对应的可执行规则来暴露它。两条路 —— 把词典条目做成 `zh-word` 家族的规则让它真能报，或者认定这个词在技术语境下可用、从词典里撤掉。**先定哪一条，再动那 38 处**，不要反过来。

## 实现与分发

- **Rust 全量迁移**：[ADR-0015](adr/0015-rust-migration.md) 的阶段 A 已完成 (2026-09-06，验收见 [PR #105](https://github.com/Luolc/limae/pull/105))：临时 `limae-rs` 已实现全部确定性规则、Markdown 保护、配置、行内指令、文件选择与写回；共享黄金集和 Python / Rust 真实进程差分通过。配置错误按 [PR #75 的协调决定](https://github.com/Luolc/limae/pull/75#issuecomment-5558344043) 比较退出码、类别、来源路径、键与既有手写消息子串，保留诊断而不要求回显未校验值。完整质量门见 `AGENTS.md`。B1 已在 [PR #108](https://github.com/Luolc/limae/pull/108) 完成 Cargo 源码包解包测试与安装、离开源码后的独立运行，以及 Linux GNU / musl 业务验收；分发与安装验收见 [分发说明](knowledge/rust-binary-distribution.md)。B2 已在 [PR #110](https://github.com/Luolc/limae/pull/110) 完成独立 `limae-rs` pre-commit opt-in 与原 Python 入口的隔离安装、检查及修复验收；阶段 B 完成，十二条门已随 #111 同步。C1 已在 [PR #113](https://github.com/Luolc/limae/pull/113) 完成润色子进程的受控执行与回收，区分取消与超时并限制输出；每次已启动调用均承担完整终止宽限，清理在捕获期限之外，具体 API 合同见 `rust/polish/process.rs`。C2a 已在 [PR #115](https://github.com/Luolc/limae/pull/115) 完成 prompt 嵌入、预设与 custom 命令展开及受控调用、环境隔离、两类 answer 通道与换行归一；真实模型仍待 C3 验收。C2b 拆成四批串行，不因 C2a 纵向交付许可把后续任务合成一批。第 1 批已在 [PR #117](https://github.com/Luolc/limae/pull/117) 完成失败分类与脱敏诊断：给人看的 state 与给调用方分辨的 reason 保持 Python 的两层粒度，`_classify` 的两条正则与判定次序、spawn / timeout / 取消 / 非零退出 / 空答案 / 读不出的答案到 reason 的映射，被分类的子进程输出不进返回值、错误、`Debug` 或 source 链；取消在 Python 无对应概念，归 `other`。第 2 批已在 [PR #119](https://github.com/Luolc/limae/pull/119) 完成 auto 排序与 marker 探活：补齐 preset 元数据与有序引擎表，`probe` 以答案是否携带标记为合格判据、退出 0 而打认证错误的一律不算通过，`serde_json` 由 dev-dependency 升为运行时依赖 (锁文件无变化)。两处有意与 Python 不同、经审查方核准：传入的环境即全部环境，不回落 ambient；`probe` 把引擎的结局报成 state、把本进程自己的失败作为错误上抛。第 3 批已在 [PR #120](https://github.com/Luolc/limae/pull/120) 完成双 TTL 缓存与 `select` 全链：成功与失败各有自己的 TTL，两者的不对称依赖真实调用失败时的回写才成立 —— 许可被每一次真实调用重新检验，否决没有任何东西会去重跑，所以否决的 TTL 整个都是探测延迟；`custom` 与未安装不写缓存；排序每次重算，只有各引擎的 observed state 走缓存，不缓存「选了谁」，缓存外的回答压不过用户此刻所在的会话。缓存里只有引擎名、state 与时间戳；写不进去是静默非致命，读到畸形条目一律丢弃。第 4 批已在 [PR #121](https://github.com/Luolc/limae/pull/121) 完成 `[polish]` 配置解析与引擎优先级链：`engine` / `model` / `command` 三项与五类错误合同，优先级为命令行 `--engine` → `LIMAE_ENGINE` → 配置文件 → `auto`，复用 `rust/config.rs` 既有的向上查找与两载体处理、不另立第二份；诊断只含路径、键与静态已知名单，不回显未校验的配置值。至此 C2 完结。C3 已在 [PR #123](https://github.com/Luolc/limae/pull/123) 完成 `polish -` 的真实 CLI 接线：顺序、三个退出码与三个错误前缀对着 Python `main` 迁移，`--engine` 与 `LIMAE_ENGINE` 带进来的引擎名不经配置文件校验、故在 CLI 层再查一次，`hook` 仍挡住并退出 2 (属 D)。离线受控 stub 测试进门；真实引擎 smoke 不进 required check，判据是**逐字前后对照**而不是「退出 0 且 stdout 非空」 —— 后者对「模型改写了」与「原样回吐」给出相同输出，而 `spec/polish/general.md` 的 `If the text needs no change, output it unchanged` 让原样输出成为一条合法出路。该 smoke 只证明改写发生，不证明改写质量；语义层裁判仍是 ADR-0008 §八，未做。至此 C 阶段完结，余 D 与 E。排序合同以 [PR #116 的澄清](https://github.com/Luolc/limae/pull/116#issuecomment-5562511070) 为准：binary 缺席是唯一的硬排除、不是排序键，已安装候选中 host 优先、credential 痕迹随后、其余最后。依赖与验收合同见 ADR。D1a 已在 [PR #126](https://github.com/Luolc/limae/pull/126) 完成 hook 会话状态层迁移到 `rust/hook/state.rs`：会话目录与诊断行、分片写入 (`keep` / `part`)、过期判定与 prune；文件 0600、目录 0700，且 mode 在 create 调用里而不是事后 chmod —— 两步之间那份文件全机可读。非 Unix 平台的处置用 `const MODES: bool = cfg!(unix)`，`create_directory_with_modes` 在 `modes == false` 时于任何 mkdir 之前返回 `ErrorKind::Unsupported`。审查方曾建议加一条非 Unix 的编译臂，实现方拒绝并给出理由 (那一臂在修复不在场时不会红)，orchestra 支持了这个拒绝；同轮 orchestra 裁掉了搬运 `SIBLING_WAIT` / `SIBLING_POLL` —— 那一批里它们没有消费者，而没有消费者的常量没有任何一臂能在它错了时变红。D1b 已在 [PR #127](https://github.com/Luolc/limae/pull/127) 完成改动计数与 diff 显示迁移到 `rust/hook/render.rs`，对齐算法是 CPython `difflib.SequenceMatcher` 的端口；配套 `tools/render_diff_cases.py` 用 Python 参考实现跑一个固定合成语料、把答案写成 `rust/hook/render_cases.rs`，Rust 测试回放它 —— 判据不是「两个 diff 看起来都合理」，而是逐条对齐参考实现的答案，因为改动条数就在用户屏幕上；合并前与 main 冲突，按「审查方已记录 head 则用 merge 不用 rebase」处置，冲突只在 `rust/hook/mod.rs`。CI 第十三道门已在 [PR #128](https://github.com/Luolc/limae/pull/128) 把 `uv run python tools/render_diff_cases.py --check` 接进 CI 与 `AGENTS.md` 的门列表，十二道变十三道；这道门补的是一个「看不见被检查对象」的绿灯：改了 `src/limae/hook.py` 的计数或对齐而忘了重跑生成器时，`render_cases.rs` 停在旧答案上，而 `cargo test` 照样全绿 —— 它回放的正是那份陈旧语料。验收给了三臂，并区分了它们证明的东西：干净工作区退出 0；**改参考实现、语料不动**退出 1 (这一臂才证明门能发现语料与参考实现脱节)；手改语料一个字节也退出 1 (较弱，参考实现没动过它也会红)。审查方另外实测了「改参考实现那一臂下 `cargo test` 仍绿」，把实现方原本标为机制推论的那一句变成了实测。D2a 已在 [PR #130](https://github.com/Luolc/limae/pull/130) 完成 `src/limae/ab.py` 整体迁移到 `rust/hook/ab.rs`：A/B 抽样与码名登记、两候选并发调用、盲评渲染、账本与单次润色记录 (ADR-0012)、`pending.json` 的一次性播报。账本与参考实现**逐字节一致**，基准取自实跑 Python 参考实现，含结尾无换行、文件 0600、目录 0700。保真处两条：失败原因按 (a, b) 的**列序**而非完成时间上报；两个答案在这一层 strip，使屏幕上那份与账本里那份是同一个字符串。盲评写成成对臂 —— 反臂断言两个 engine 与两个 model 名不出现在渲染与播报里，正臂断言同一次 trial 的它们确实在账本里；只有反臂的话，一个什么都不输出的实现也能通过。17 个变异中 15 个被具名测试抓住，2 个作为可证明的等价变异留活、未为其编造测试 (为等价变异硬写断言，写出来的是恒绿的空洞)，等价性论证经审查方独立复核。实现方在此批发现并修好一个测试台自身的洞：引擎子进程拿到的是被过滤后的假 PATH，里面没有 `sleep`，第一版并发汇合脚本因此秒退、并发那一臂什么都没测到。D2b 已在 [PR #129](https://github.com/Luolc/limae/pull/129) 完成 `_tidy` / `_assemble` / `_number` 迁移到 `rust/hook/parts.rs`，`SIBLING_WAIT` / `SIBLING_POLL` 随其真正的消费者一并回来；`_keep` 不在此批 —— 它在 D1a 就已落地，不写平行实现。三处经裁决的差异：`assemble` 读盘失败返回 `Err` 而不是 `None`，因为吞成 `None` 会让诊断行说「有片没到」而其实到了，是一条会说谎的记录；stderr 走 `&mut dyn Write` 参数而非 `eprintln!`，本仓 deny 了 `print_stderr`，且这让那行在测试里可断言；`tidy` 返回 `Option<Kind>` 而非 Python 的空串哨兵。`number` 的数字语法比 Python `float()` 窄 (PEP 515 下划线与非 ASCII 数字落到 fallback)，**有意保留**：方向是退回文档写明的默认值，与 Python 对一个手误的处置同向，补那些分支正是本仓不要的防御性负向断言。审查方在复审中抓到一条 P1：注释里的实测枚举有多项没有对应测试臂 —— 一段写着「实测对照」却没有任何东西在它错了时变红的注释，是一条等着说谎的注释；裁决取「把注释缩到已覆盖的输入」而不是「为枚举补臂」，理由同上，`U+001C`–`U+001F` 那句「未实测」保留，它标的不是承诺而是一个已知的未知。`assemble` 返回的 `Err` 必须由接线宿主的那一批映射到与其它 crash 相同的 fail-open 路径 —— 诊断行记 `Kind::Crashed`，屏幕上留用户自己的字，Python 那里是靠 raise 走到 `main` 的兜底；这条已经写进 `rust/hook/parts.rs` 的 `# Errors` 段，tracker 这里只记它是 D 剩余部分的验收项。至此 hook 的状态层、显示层、A/B 与分片装配均已落地。D3a 已在 [PR #132](https://github.com/Luolc/limae/pull/132) 完成块构建层迁移到 `rust/hook/block.rs`：`_single` / `_shown` / `_one` / `_trial` / `_block`，即「这条消息该显示什么」；同批把 `rust/polish/engines.rs` 的 `HOOK_DISABLE_VARIABLE` 由私有改 `pub`，`block.rs` 用它往子进程环境塞禁用标记以防 hook 递归。ADR-0012 的核心判断落地：修复没跑起来的那一轮**不进样本** (它的 `displayed` 会等于原文，而那是「规则没跑」不是「模型没写出要清理的东西」)，但仍然上屏、仍然留 `fix` 诊断行。`_one` 的三种返回分开保留 —— 「无改动」/「N 处改动」加全文 /「仅排版改动」，最后一种的理由是确实动了东西但动的只有空白与标点，而那一层归确定性规则管，说「无改动」是假话。**这一批的实现方中途被 orchestra 误关** (收尾另一任务时未核 tab id 就发了关闭命令，`herdr tab close` 的 `ok` 对「关对了」与「关错了」给出相同输出)，代码留在盘上、无提交；接手方按「继承来的、没有作者可问的改动」审而非假定其正确，通读后未推翻任何一段，但拿到硬证据说明前任停在半路 —— **前任没跑过第四道门**，`cargo doc` 退出 101，模块头两处 intra-doc link 指向私有项。接手方另补三处测试空洞：A/B 账本的测试原本只断言文件存在，把 `text` 与 `displayed` 对调不会红；`trial` 那条的桩答案本身没有可修之处，「修没修过」在它上面没有分辨力；`record` / `record_run` 的 OSError 路径原本一条测试都没有。

  探针时限已在 [PR #133](https://github.com/Luolc/limae/pull/133) 修复：`PROBE_TIMEOUT` 定义在 `rust/polish/select.rs` 却无人使用，已合入的 CLI 路径拿 `EngineLimits::default()` (RUN_TIMEOUT) 去做引擎探活 —— 用「等一次改写」的时限做探针，与参考实现分叉 (Python 的 `engines.select(env)` 根本不收 timeout，内部按 `PROBE_TIMEOUT` 探)。**修法落在 `select` 内部而非调用点，是实现方拒绝 orchestra 原方案后改定的**：只改调用点写不出有分辨力的测试 —— 那条路径上没有 timeout 旋钮，600 秒与 90 秒要分开就得让探针睡满 90 秒，而任何更快的写法在改动还原后照样绿；orchestra 否决了「照改并把测试降级成常量断言」这个退路，那等于为迁就 brief 而放一道假门。修在源头同时更贴参考实现的形状。`limits` 未变成被忽略的参数：`EngineLimits { timeout: PROBE_TIMEOUT, ..limits }` 保留 caps、只覆盖 `timeout`，doc 注释如实写明哪一半适用。审查方的两臂做法比实现方更严谨 —— **把那条测试单独移植到父提交上跑**，证明的是「这条测试在修复不在场时会红」，而不是「改坏了什么之后它会红」。

  D3b 已在 [PR #134](https://github.com/Luolc/limae/pull/134) 完成宿主协议层与入口迁移到 `rust/hook/cli.rs`，`rust/cli.rs` 里原本挡住 `hook` 并退出 2 的那一处换成真接线 —— 至此 `hook` 子命令真正可用。三种线上输出形状、`pending` 只播报一次 (一个总是返回内容的 `Stop` hook 会自触发，实测把「只说 hello」变成五轮不停)、末尾空行只能数不能收回 (`displayContent` 只替换最后一个 delta)。落成一个模块而非三个，理由是 `_output` 一个函数分派三个事件、`_is_codex_stop` 是把其中两个分开的那句判据，三分会把接缝划在同一个决定的中间。**上一批欠下的跨批次契约在此兑现**：`parts::assemble` 的 `Err` 走与其它 crash 相同的 fail-open 路径，记 `display` / `crashed`；钉住它的两条测试互为对照，而**两次运行在屏幕上完全同形 (都空屏、都退 0)，所以诊断行是唯一有分辨力的观察**。审查方抓到一条 P1：`index = usize::MAX` 时 `index + 1` 溢出，debug 下 panic 退 101，违反 ADR-0009 六节。orchestra 另核出审查方没测到的一半：**`Cargo.toml` 没有 `[profile]` 段，release 沿用 Cargo 默认的 `overflow-checks = false`，而分发门构建的正是 `--release`** —— 同一行代码在 debug 下 panic、在 release 下回绕成 0 并悄悄走出错误结果，而 release 是分发出去的那个二进制。因此测试断言的是 fail-open 的具体结果 (屏幕空、零次模型调用、状态目录不存在)，不是「没有 panic」 —— 后者在 release 下恒绿。同批把两处不可达的减法改成 `saturating_sub` 但**明确不加断言**，理由是构造不出触发条件的断言正是伪装成门的空洞。

  assemble 的无界分配已在 [PR #136](https://github.com/Luolc/limae/pull/136) 修复：`parts::assemble` 原本用 `(0..batches)` 一次性铺开每个 batch 一个 `PathBuf`，而 `batches` 直接来自 payload 的 `index + 1` —— 由宿主发来的一个数字决定分配多少内存。缺陷来自 PR #129，由 #134 的实现方发现并上报，单起一批修复。**修法取「改读 parts 目录」而非「给 batches 设上限」**：后者需要一个规范里没有的常量，而「一条消息最多 N 批」是 ADR-0009 该说的句子、不是实现能发明的；凭空造一个 N 要么小到误伤真实消息、要么大到根本没挡住，而两者都没有任何东西能告诉我们选错了。修复前后实测 (debug 二进制，真走 `limae-rs hook` 的 MessageDisplay payload)：`index=1e6` 前 10.33 秒 / 139 MB → 后 2.01 秒 / 6.7 MB；`index=1e7` 前 1 分 34 秒 / 1.33 GB → 后 2.07 秒 / 6.1 MB；**`index=1e9` 前退出 134 (分配失败 SIGABRT) → 后 2.02 秒 / 6.2 MB / 退出 0**，最后一行是直接对着 ADR-0009 六节的证据。修复后的两秒是 `SIBLING_WAIT` 本身，与批数无关。实现方第一轮变异里 **M2 (只看文件个数不看索引) 无人抓住，是一个真洞**：当时 `cached` 先按 `index < batches` 过滤，索引检查因此退化成冗余、纯计数与它等价；改成 `cached` 不过滤、`whole` 正向断言前 `batches` 项索引恰好是 `0..batches` 之后，索引检查才变成承重那一句。

  E1 已在 [PR #135](https://github.com/Luolc/limae/pull/135) 完成 `tools/render_lexicon.py` 的 HTML 生成迁为 Rust 开发工具，落成 Cargo example `render-lexicon` (`rust/examples/render_lexicon.rs`)，`diff-probe` 已立先例、开发期工具进 example 而发布的二进制集合仍只有 `limae-rs`；样式表写成 `.rs` 里的 raw string 而非旁边的 `.css`，因为 `Cargo.toml` 的 `include` 只带 `.rs`。Python 脚本按 tracker 的既定要求原样保留作参考，未增加任何新输出。**验收判据是同源生成产物逐字节一致**，基准取自 Python 参考实现的输出与仓内已提交的 `site/index.html`，不是 Rust 自己的输出 (拿自己的输出当基准是一条恒绿的断言)；比法是在两个互不相干的临时目录里各跑一遍再 `cmp`。同批带来 `tools/check_lexicon_render.sh`：**它跑两轮，分辨力在第二轮** —— 只在一份固定输入上看到两个生成器一致，与「两个都不读输入的生成器」给出相同输出，所以第二轮往源里追加一个故意挑难的词条 (带声调要排到最前的拼音、要转义的 `&` `<` `>` `'` `"`、一段反引号 code span)，要求两者仍一致**且结果必须与已提交页面不同**。这一批**不适用「不追 Python 细节平价」那条标准** —— 那条松的是实现细节，而这里逐字节一致的是产物，`site/index.html` 是提交进仓的生成物，读者是人与工具、字节即证据。实现方另有一处自陈：中途曾用 `git commit -c core.hooksPath=.git/hooks` 提交，而 worktree 里 `.git` 是文件不是目录、那条路径不存在，**等于跳过了全部钩子包括 gitleaks**，发现后 `git reset --soft HEAD~1` 重提；orchestra 核实分支历史里没有残留那次提交。

  CI 第十四道门已在 [PR #137](https://github.com/Luolc/limae/pull/137) 把 `tools/check_lexicon_render.sh` 接进 CI 与 `AGENTS.md` 的门列表，十三道变十四道，step 落在 `Hook render corpus is current` 之后 (它要 `cargo build` 一个 example 并跑 `uv run`，故须在 `Sync deps` 与 Rust toolchain 之后，但不需要 musl target 与 `limae-rs`，故不必推到分发门末尾)。补的是与第十三道门同族的空白：现有各门没有一道读 `site/index.html`。**验收要求两种反臂分开造**，因为它们是两种不同的故障：① 改了 `spec/lexicon/zh.toml` 而忘了重新生成页面 → 退出 1、报页面过期；② 只改一个生成器使两者分叉 → 退出 1、报两个生成器不一致。两臂命中脚本里不同的断言，只造其中一种则这道门对另一种有无分辨力是未知的。另需分清：**脚本内部自带的对照臂是脚本自己的分辨力，不等于这个 step 在 CI 里有分辨力** —— 后者要证明的是「故障发生时这个 step 会让 job 变红」。

  用户 2026-09-07 裁决：**不追 Python 细节平价**。原话是「因为 Python 要被 deprecated 所以不需要所有细枝末节都完全 match，有些东西如果为了 match 要多很多繁琐的实现的话就舍本逐末了，rust 尽量用自己的最佳实践就可以」；随后追加「像 json 是否需要 key order 完全相同这种事情，你按照 best judgment 去做就行」，即**此类由 orchestra 直接裁决，不上交用户**。判据不再是「与 Python 是否逐项一致」，而是两问：这处差异落在契约上还是实现细节上？消除它的代价是不是「多很多繁琐的实现」？**松的是实现细节的一致性，不是验收强度** —— 判据的分辨力、ADR 定的契约、用户可见的输出，以及「差异必须在代码注释里如实写清」(不许因为「反正要 deprecated」就不写)，这四样照旧。**由此定下一条可复用的分界：看这份输出的读者是谁** —— 读者是人或工具、字节即证据的 (生成产物、黄金 fixture、A/B 账本) 要求逐字节一致；读者是解析器或另一个程序的 (线上 JSON、进程间协议) 用各自语言的最佳实践。按此，#134 的五处差异全部接受：bool index guard 不照搬 (Python 那句存在的唯一理由是 Python 里 `True` 是 `int`)、超出 `u64` / `usize` 的 index 被拒、线上 JSON 键序按字典序、stdout 写失败仍退 0 (Rust 更贴 ADR-0009 六节，Python 那处是它自己的 bug)、cwd 改为参数。

  `polish::engines::tests` 的偶发红，保持 open 未结案。2026-09-07 一天内同一模块两次：`empty_answer_is_rejected_and_temporary_resources_are_cleaned` 一次、`failures_and_debug_output_do_not_echo_request_or_child_content` 一次 (后者红在 `assert!(error.source().is_none())`)，均重跑即绿、均未归因。**不写成「已确认无问题」**：「重跑 N 次没复现」对「本来就没问题」与「低频竞态」给出相同输出。两个数据点落在同一模块、都涉及子进程与临时资源清理、都带较短时限，这不构成归因，但把「单次偶发不值得追」变成了「值得一次专门的隔离实验」，而那个实验的目标是**造出复现**而不是证明它不复现。第三次出现在 [PR #139](https://github.com/Luolc/limae/pull/139) 的第一轮全量，门 12 (`tools/check_rust_package.sh package` 里那次 `cargo test --locked`) 退出 101，红在 `failures_and_debug_output_do_not_echo_request_or_child_content` 的 `assert!(error.source().is_none())` —— **首次有机制假说**，但它是机制推断、**不是**隔离实验：`limits()` 的 timeout 是 2 秒，期望的 `EngineError::Exit` 不带 source，而超时 / spawn 失败走 `EngineError::Process { source }` 带 source；本机 4 核，门 12 紧跟门 11 的 release 编译之后跑，负载高时会踩到那个时限。实现方用 12 个忙循环试着直接复现、**没复现出来**。三次都在同一模块、都涉及子进程、都带较短时限。下一步仍是一次专门的隔离实验，目标是造出复现；因 Codex 额度受限，本轮不起批，条目保持 open。

  至此 hook 的状态层、显示层、A/B、分片装配、块构建层与两个宿主 (`_display` / `_stop` / `_codex_stop`) 及入口均已落地，D 阶段完结；E 阶段已完成 E1 (页面生成器) 与其 CI 门，`docs/tracker.md` 第 17 行「Rust 迁移收尾补项」记的其余 E 待办仍未关闭。继续自动推进 E、agent 互审，直到正式 `limae` 为 Rust、Python deprecated 且参考实现与测试保留；实际 tag / GitHub release / `cargo publish` 前停止。

  E2 已在 [PR #139](https://github.com/Luolc/limae/pull/139) 完成入口切换：Rust binary `limae-rs` → `limae`，Python 的开发 / reference 命令 → `limae-python`，`.pre-commit-hooks.yaml` 的默认 `id: limae` 从 Python 换成 Rust、Python 那个改为 `limae-python`、迁移期的 opt-in `id: limae-rs` 删除，仓内 dogfood 与 `.codex/config.toml`、CI、十四道门列表、README 跟着切换；ADR-0015 的硬约束「不通过 stdout 弃用横幅破坏协议」已遵守。`docs/adr/0015-rust-migration.md` 6 处与本文件 4 处的 `limae-rs` **有意保留** —— 它们是描述过渡期的历史事实，改掉等于说当初决定的就是终态，记录会变成假话。全新消费仓两臂 (PR #66 复审留下的 P2) 复用 `tools/check_rust_precommit.sh` 而非平行再写：rust 与 python 两侧各有 install / violation / fix / clean 四臂，**违规臂真的存在** —— 只测「装上了、退出 0」的话，一个什么都不检查的钩子同样全绿；PATH 上放了同名探针，保证跑的那个来自 pre-commit 自己的缓存。差分臂用**三臂**证明它没有静默退化成「跑两遍同一个」或「只跑一个」：注入一处 Rust 侧变异后，Python 臂绿、`--rust-bin` 臂红 5、dogfood 钩子错误地绿。实现方另**主动撤掉**了一处自己想加的断言：本想把 `rust/tests/cli.rs` 的 `Usage:` 断言收紧成 `Usage: limae `，发现对照臂是绿的 —— clap 的 usage 行用 `bin_name` (来自 `argv[0]`)，`Command::new` 的名字在这条路径上根本不显示，那条断言实际断言的是「二进制文件叫 limae」，而 `CARGO_BIN_EXE_limae` 已经保证了这件事；按「回归测试必须在修复不在场时会红」撤掉，没留下看着像门的空洞。dogfood 钩子用 `cargo run --locked --bin limae --` 而非 `target/debug/limae` (后者在 binary 是几周前建的时候照样全绿，那道门检查的就不是此刻的规则了)，并加 `require_serial: true` 避免并发撞 Cargo build 目录锁；`.codex/config.toml` 反过来用已建好的 binary，因为每批新行起一次进程且各批并发派发，`cargo run` 会把它们串起来。

  **发布元数据缺口** (本 PR 修正)：`Cargo.toml` 的 `[package]` 此前没有 `description`，而 crates.io 对它必填 —— 今天这份 manifest 发不出去，所以下面「Rust 发版准备」条里那句「发布所需的缺口至此已全部关闭」在写下时就不成立，已一并改准。本 PR 补 `description` / `repository` / `readme` / `keywords` / `categories`；`publish = false` 不动，它仍是既定停止点。**判据上的教训一并记下**：`cargo publish --dry-run` 对「crates.io 会不会接受这份 manifest」**零分辨力** —— 上一批在 scratch 副本里把 `publish` 改 true 跑过 `cargo publish --dry-run --no-verify --allow-dirty --locked`，退出 0，只打印 `warning: manifest has no description…`；它根本没走到服务端校验，接受与拒绝两种情况下都退出 0。今天能给的证据只有「这些字段确实在 manifest 里、且 `cargo package` 打得出来」，真正的接受与否要到实际发布那一步由 crates.io 给出，那不在本轮授权内。

  **跨仓消费方** (只调查未改，ADR 允许的范围)：已知三处都钉在 `v0.11.0`，今天不受影响。`machine-setup` 的 `check.sh` 用 `uvx --from git+…@v0.11.0 limae --all`，一旦 bump 越过这次改名，`limae` 这个 console script 不再存在，会**硬失败**；`butler` 与 `wealth-management` 的 `id: limae` bump 后变成需要 Rust 工具链的 hook。已由 butler 亲验并交 machine-setup 记一条待办，触发点挂在「limae 发布首个 Rust 入口版本」。**调查限度一并记**：代码搜索只覆盖公开且被索引的仓，私有仓看不见，**查不到不等于不存在**。
- **Rust 迁移收尾补项**：E 的默认 pre-commit id 切换必须包含 `.pre-commit-hooks.yaml` 的 `language` / `entry` 与全新消费仓安装验收 (PR #66 复审 P2)。orchestra 按用户「当前 Python 全部功能迁移」的范围对账 (2026-09-06)，补入现有 `tools/render_lexicon.py` 的 HTML 生成功能：迁为 Rust 开发工具，以同源生成产物字节一致验收；Python 脚本保留作参考，不增加 rubrics 等新输出。
- **后续分发形态**：多语言 SDK、LSP、编辑器与 CI 集成，对标 AutoCorrect；Rust 迁移完成后另起 ADR，不属于本轮 A–E。
- **Rust 发版准备** (2026-09-07)：许可证已由用户 2026-09-07 裁决为 **Apache-2.0** (单许可)，全文按用户指令从既有仓库逐字节复制而非生成，appendix 的版权行保持占位符原样，`Cargo.toml` 与 `pyproject.toml` 的 SPDX 值同为 `Apache-2.0`，`Cargo.toml` 的显式 `include` 已含 `/LICENSE` 并由打包门验收 (见 [PR #124](https://github.com/Luolc/limae/pull/124))。许可证与打包内容这两处缺口至此关闭，但**发布所需的缺口并未全部关闭** —— `Cargo.toml` 当时还缺 crates.io 必填的 `description`，见上面「发布元数据缺口」一段。停止点不变：`publish = false` 未翻转，实际 tag / GitHub release / `cargo publish` 仍在本轮授权之外。[维护者的开发机发布手册 (私有仓)](https://github.com/Luolc/machine-setup/blob/main/docs/knowledge/crates-io-publishing.md) 已经 machine-setup #194 合入；crates.io token 用户已创建，引用 `op://dev-shared/crates-io-publish-token/credential`，发布按手册 §4 经 `op run` 注入，首发后配置 GitHub Actions Trusted Publishing。2026-09-07 经 butler 转述的 dev-oregon 只读验收：引用存在、`op run` 能注入、本机无 credentials 文件；该轮另报 token 无过期、scope 含 publish-new 与 publish-update 且不限 crate —— 这两项 limae 侧未亲见，记作转述。**这些都只证明取到了东西，不证明 crates.io 会接受它**：可用性要到真发布那一步才由消费方给出，届时仍只回显状态、不读取凭证值。操作与权限配置以手册为准，不在此重复。该待办不阻塞代码迁移与无需认证的发布预演，实际发布仍在本轮授权停止点之外。

## 愿景 (正本 `docs/adr/0005-agent-native-positioning.md`，这里只记条目)

- **LLM 语义润色**：agent 调用的语义层润色特性，与确定性 lint 互补。
- **heuristic learning**：从润色前后 pair 蒸馏 experimental 规则 (术语选词、破折号过量等)。
- **website 与多语言文档**：对标 AutoCorrect 的站点与多语言文档。
- **社区贡献机制**：CONTRIBUTING 等贡献流程。

## claudish 调研产出 (`docs/research/claudish-and-ai-slop-survey.md`)

- **文档级密度规则**：破折号 / 粗体 / 列表化行文的密度判定是文档级的，`.findings` 的「行号 + 规则 id」形制装不下；要先给规范加文档级 finding 的形制，再收这批 (ADR-0007 §三)。
- **英文 tells 词表**：English-to-English 的实验规则 (`load-bearing` 过量、否定平行的英文形态等)，沿用 tell 家族 (`zh-tell` / `en-tell`) 与 `spec/wordlists/` 形制；规则不分语言，出现在哪管到哪 (ADR-0006 §五)。
- **A/B 单侧失败时降级**：两个候选只回来一个时，现在整轮不显示；应改成降级成单路并把失败记进诊断，而不是整轮消失。根在 `ab.run` 要改成逐候选返回。A/B 关着时不发作。
- **型号准入清单**：`ab.py` 里一份可执行的合格型号表，默认空；证据在 `docs/research/polish-engine-cli-behavior.md`。判据必须是该文档 §六 第 2 步的实测，**不能是裸 PONG** —— haiku 与 sonnet 裸 PONG 都通过，而它们正是 #46 里把正文当对话的那两个 (ADR-0008 §四)。
- **润色的方差**：同一段输入连跑四次，输出从「逐字完全相同、一字未改」跨到「删光全部行内强调并引入一个 `zh-typography-1`」(2026-09-01 实测)。两端都不是润色。`spec/polish/general.md` 的「If the text needs no change, output it unchanged」给了「什么都不做」一条合法出路。先按 ADR-0012 的单路记录攒真实分布，再决定改法 —— 单次采样证明不了任何事。
- **ADR-0008 §七 的澄清缺指向**：§七「不做旁路目录」被 ADR-0009 §五 澄清过，但 `0008-limae-polish-cli.md` 状态节没有指向；§十 已由 ADR-0012 补上同类的一句。将来有任务碰这份 ADR 时顺手补齐，不单独开 PR。
- **`polish` prompt 的 evolve**：两份 spec 已落地 (PR #37)，但还是起步版；按 ADR-0008 §九 走 alpha-evolve (演化式迭代)，靠 P0 那个 hook 的反馈驱动，并带上 §五 记的那条社区证据对 prompt 措辞的影响。在方差那条解决之前谈不上调「够不够狠 / 像不像作者 / 有没有改错地方」 —— 那三种病要在输出稳定之后才分得出来。
- **引擎实测随版本复核**：`docs/research/polish-engine-cli-behavior.md` 记的三家 CLI (command-line interface) 实测行为带实测日期，CLI 升级或换型号时按该文档的人工验收步骤重跑，通过才进预设表 (ADR-0008 §四)。
- **结构不变量保护器**：改写前后逐字比对围栏、行内代码、链接目标与锚点、标题行、表格结构，变了就判不合格；另配语义层的模型裁判 smoke test，永不进 CI (ADR-0008 §八)。§八 写的是「**至少**要包括」，所以还要补上**行内强调标记** —— 实测模型会把整段 `**` 吃掉，而清单里今天没有这一项，也就是说它并未违规。同时把这一级从 §十 排的 P1 提前到 hook，理由是 hook 才是每条消息都在走的活路径。另有旁证说明清单本身不够：#46 里 haiku 删掉一整段，违反的是**已经写在清单里**的块结构那条 —— 请模型别删要做，我们自己数才是兜底。
- **`polish` 默认模型由 A/B 决定**：ADR-0008 §五只记候选与判据，暂定 terra / sonnet / grok-4.6；用自家语料做 10–20 条盲对照后再定，降到便宜档必须有自家证据。
- **`limae` 三个子命令的实现**：`check` / `format` / `polish` 的命令行分层 (ADR-0008 §二)，今天的 `limae [--fix]` 在过渡期继续可用。
- **P1 `polish` 的文件形态**：单文件改写，以及按 git 变更集 (dirty 或最近一个 commit 碰过的 Markdown) 批量；P2 再做跨文件协调改写 (ADR-0008 §十)。
- **zh-tell-5 补「零 + 拉丁 / 混合名词」**：现判定只取「零」右侧的连续汉字串，漏掉「零 SA 需要读它」「零 service account 需要读它」这类「零 + 拉丁或中英混合名词 + 谓语」的形态 (`machine-setup` 2026-08-31 用 v0.9.0 跑改写前语料时发现，当次靠人工改写)。匹配单位要扩到拉丁词与混合串，边界与白名单语义随之定案。
- **实验规则的 per-file 豁免写进规则文档**：各仓的 tracker / journal 这类历史账按约定不改写，zh-tell-5 / zh-word-2 将来若转 stable，这些文件需要整份跳过。`.limae-ignore` (v0.5.0) 已经能做，缺的是在规则文档里写明这条建议做法。
