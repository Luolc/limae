# 运行记录：AGENTS.md，2026-09-25

| 项 | 值 |
| --- | --- |
| 基线 commit | `8fad2f9` |
| 命令 | `limae polish --share-repo-with-engine AGENTS.md`，随后 `limae --fix AGENTS.md`、`limae AGENTS.md` |
| 引擎 | `auto` |
| prompt / 词典最后改动的 commit | `ce08b10` (`spec/polish/`) / `3bb36db` (`spec/lexicon/zh.toml`) |
| 目标 | 1 个文件 (`AGENTS.md`)，第一次 `limae polish` 即通过结构核对，进仓 |
| 改动量 | **两个口径**都是 `before/` 到 `after/` (模型纯产物) 与 `before/` 到进仓文件 **11 增 11 删**——同一批 11 行发生变化，进仓文件在其中 8 行上叠加了手工修正 (见下)，但改动的行数没有变 |
| `--fix` 是否动过 | 否——`limae --fix` 对 polish 产物报 `OK: 1 file(s) clean`，无额外改动 |

`CLAUDE.md` 是指向 `AGENTS.md` 的软链接，未改动。

## 结构核对 (标题 / 围栏行 / 列表项 / 总行数)

下表按 `rust/polish/cli.rs::Shape::of` 统计 (用与该函数逐字一致的独立小程序复算，未接入正式 crate)：围栏行按开合各计一行，列表项含 `-` / `*` / `+` 与有序标记。

| 文件 | 标题 | 围栏行 | 列表项 | 行数 |
| --- | --- | --- | --- | --- |
| `AGENTS.md` | 8 → 8 | 2 → 2 | 27 → 27 | 84 → 84 |

四项计数在 `before.md`、`after.md` (模型纯产物) 与进仓文件三者之间全部相等，第一次跑就通过结构核对，没有被拒绝、没有重跑。

## 反例原样保留

「门槛」(阈值) 不是「门」这个比喻要改的对象，第 26、57 行「判据与门槛」两处模型均未动，与 brief 的要求一致。

## 手工修正

**这批有手工修正，且只针对「门」这一个比喻的具体措辞**：brief 要求「不用『门』这个比喻：说一组检查用『质量检查』，说单条用『检查』，说『卡在这里过不去』的位置用『关卡』，按语境挑」。词典对「门」这个词条给出的候选是「检查；关卡；测试」，模型这次全部选了「关卡」，没有按语境区分——这不是「原文更好」式的撤回，是把模型的选词收窄到 brief 明确要求的三分类上；上一批 (`2026-09-24-rules-surveys-release`) 处理同一个词时就分别用了「质量检查」「一项检查」「一条判据」「打包检查」等不同词，这批延续同一口径。

逐处修正 (进仓文件的行号)：

| 行 | 语境 | 模型产物 | 修正为 | 理由 |
| --- | --- | --- | --- | --- |
| 27 | `check_skill_render.sh` 单条检查 | 那道关卡 | 那项检查 | 单条 |
| 33 | CI 覆盖的十四项检查 (整体) | 十四道关卡；…关卡顺序裸跑 | 十四项质量检查；…检查顺序裸跑 | 一组 |
| 56 | 十四项检查里的两道 pre-commit (整体的子集清单) | 关卡列表 | 检查列表 | 一组 (清单) |
| 57 | `lint-lexicon` 单条检查 | 那道关卡 | 那项检查 | 单条 |
| 58 | `lint-lexicon` 单条检查 (×2)、清单 (×2)、单条描述 (×2) | 那道关卡 / 门列表 / 写成…的门 / 像一道门 | 那项检查 / 检查列表 / 写成…的检查 / 像一道检查 | 单条与清单 |
| 60 | 打包场景的单条检查 | 打包门里永远绿 | 打包检查里永远绿 | 单条 (与上一批「打包检查」一致) |
| 60 | 描述关卡通过 / 不通过的状态 | 改坏 → 门红，改回 → 绿 | 改坏 → 关卡红，改回 → 关卡绿 | 「卡在这里过不去」的状态描述，保留「关卡」 |
| 61 | Rust 侧检查的整体小标题 | **Rust 质量门** | **Rust 侧的质量检查** | 一组 |
| 61 | 分发相关检查 (子集) | 分发门 | 分发检查 | 单条/子集，与「打包检查」同构 |
| 61 | CI 装 target 这一步不计入清单 | 不算一道门 | 不算一道检查 | 单条 |
| 74 | 与第 33 行同一处「十四项检查」整体的复指 | 十四道关卡 | 十四项质量检查 | 一组，与第 33 行保持一致 |

逐份读过 diff，除上述「门」的措辞外，没有发现语义被改、反例被改，或残留「门」的比喻。其余排版与措辞改动 (如「兜住…」→「堵住…这两个缺口」、「正本」→「源文件」这类命中词典的替换) 均照原样保留，未做撤回。

## 裁决

留白。裁决人是用户；判据不在这里定义，用户怎么说就是什么。记裁决时写在下面，注明日期，照抄原话不要改写。
