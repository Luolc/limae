This release is a testament to a pivotal shift.

The evolving landscape of a diverse array of tools.

这一段的英文 tell 是 tapestry 和 testament，中文文档里同样报。

The `testament` in `tapestry` is code, not prose.

See https://example.com/testament/tapestry for details.

The is_pivotal column of the table.

This is not just a linter, but a spec.

It's not a detector, it's a rule floor.

The point isn't the words — it's the plan.

It's not X, it's Y; and that isn't A, it's B.

Not only fast but also correct.

The rule is not new, and after a detour long enough to blow the window it is fine.

This constraint is load-bearing.

A load-bearing check and a load-bearing default.

Read the `load-bearing` flag from the config table.

See https://example.com/load-bearing/notes for the plan.

<!-- limae-disable-next-line en-tell-1 -->
We keep another tapestry of tests.
