# 运行记录：清掉「门」这个词的一批，2026-09-16

| 项 | 值 |
| --- | --- |
| 基线 commit | `1c97684` |
| 命令 | 逐个 `limae polish --share-repo-with-engine <file>`，全部跑完再一次 `limae --fix <全部>` |
| 引擎 | `auto` |
| 目标 | 11 个文件，**其中 1 个被撤回** (见下)，进仓的是 10 个 |
| 改动量 | 10 个文件，478 增 478 删 |
| `--fix` 是否动过 | 是 —— 修了 `docs/knowledge/rust-binary-distribution.md` |

## 这一批最重要的读数：polish 把一份文档砍掉了一半，而所有检查都是绿的

`docs/knowledge/release.md` 从 **376 行变成 198 行**，「四、PyPI 与 npm 启动器」与「五、pre-commit 镜像仓」**两整节 15 个标题全部消失**，包括 Trusted Publishing 的逐步配置与镜像仓验收的失败探针那一段。

**它不是断在句子中间。** 产物在第三节末尾干净地收尾，读起来是一份完整文档 —— 比截断更难发现。

**没有任何既有检查看得见它**：`limae --fix` 与 `limae` 对这 11 个文件都报 `OK: 11 file(s) clean`；文件模式的绊线只看视图里有没有被写，看不到答案本身缺了什么；写回前的冲突核对只比目标文件有没有被别人改过，不比答案与原文的体量。

这一份已撤回、不进本 PR，产物留在 `~/scratch/limae/discarded-runs/release.truncated-2026-09-16.md`。

### 判据已经有了，而且分辨力是被这次真实故障验出来的

数三样：标题 (`^#{1,6} `)、代码围栏 (`^\`\`\``)、列表项 (`^\s*[-*] `)。前后必须相等。

| 文件 | 标题 | 围栏 | 列表项 |
| --- | --- | --- | --- |
| `docs/knowledge/release.md` | **25 → 10** | 2 → 2 | **22 → 12** |
| 其余 10 份 | 全部相等 | 全部相等 | 全部相等 |

**两臂输出不同，这个判据才成立** —— 坏的那份被它抓住，10 份好的一个都没误报。它不是一个凭感觉定的比例阈值，而是直接对着 prompt 已经写下的那条契约 (「Block structure: the same blocks in the same order… Add no section, drop none」) 做核对。**这条该进 limae 自己**，作为写回前的一道拒绝，与既有的冲突拒绝并列；另开任务做。

## 「门」这个词

用户 2026-09-16 裁决：不用「门」这个说法。

模型自己清掉的：38 处 → 17 处。**它做对了最难的那部分** —— 作为负面案例被引用的「门控」两处 (`0007`、`llm-polish-survey`)、以及 `0016` 里那条排版前后对照 ```**十二道门**(含…``` → ```**十二道门** (含…``` 的字面文本，一处都没动。改了这些就把例子毁了。

它漏掉的是普通的那些，手工补了 11 处 (见下)。最终残留 4 处，全部是不该改的：「门控」×2 (引用的坏词)、「十二道门」×2 (排版对照里的字面文本)。「门槛」(阈值) 与「专门」不在此列，本来就不是那个比喻。

## 合入前的手工修正

`after/` 是**模型的纯产物**，进仓文件在它之上多了这 11 处：

| 文件 | 模型留下的 | 改成 |
| --- | --- | --- |
| `docs/adr/0015-rust-migration.md` | 独立质量门约定 / 质量门约定 / 进门 / 两个 Python 裸质量门 / 已启用的质量门全绿 / 即启用门与 lint 例外 | 质量检查约定 ×2 / 纳入检查 / 两项 Python 裸质量检查 / 已启用的质量检查全绿 / 即启用检查 |
| `docs/adr/0016-hook-mechanical-only.md` | 契约门两臂 / 十三道门裸跑全绿 | 契约检查两臂 / 十三项检查裸跑全绿 |
| `docs/research/lexicon-static-site-survey.md` | 质量门列表 | 质量检查列表 |
| `docs/research/llm-polish-survey.md` | 以 CI 为质量门的工具 | 以 CI 为质量关卡的工具 |
| `docs/research/polish-engine-cli-behavior.md` | hook 持久化信任门 | hook 持久化信任关卡 |

换的词按语境挑，不统一成一个：说一组检查用「质量检查」，说单条用「检查」，说「卡在这里过不去」的位置用「关卡」。

## 裁决

留白。裁决人是用户；判据不在这里定义，用户怎么说就是什么。记裁决时写在下面，注明日期，照抄原话不要改写。
